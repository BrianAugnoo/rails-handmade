<div class="border rounded p-3 bg-light">
  <p class="fw-bold mb-1"><%= comment.user.user_name %></p>
  <small class="text-muted d-block mb-2"><%= comment.created_at.strftime("%b %d, %Y") %></small>
  <p class="mb-0"><%= comment.review %></p>
</div>
