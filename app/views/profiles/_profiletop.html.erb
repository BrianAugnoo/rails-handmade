<nav class="bg-white border-b border-gray-200 shadow-sm px-6 py-4 flex items-center justify-between">
  <div class="flex items-center space-x-4">
  <% if current_user.profile_image.attached? %>
    <%= image_tag current_user.profile_image.variant(resize_to_fill: [50, 50]), class: "rounded-circle" %>
  <% else %>
    <%= image_tag "default_avatar.png.svg", size: "50x50", class: "rounded-circle" %>
  <% end %>


    <div>
      <p class="font-semibold text-gray-800"><%= @current_user.user_name %></p>
      <div class="text-sm text-gray-600">
        <span><%= @artwork_count %> artworks</span> ·
        <span><%= @subscriber_count %> subscribers</span>
      </div>
    </div>
  </div>

  <div class="flex items-center space-x-4 relative">
    <%= link_to "Edit Profile", edit_user_registration_path, class: "bg-blue-500 hover:bg-blue-600 text-pink text-sm font-medium py-2 px-4 rounded-xl" %>

  </div>
</nav>
